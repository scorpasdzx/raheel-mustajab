fn main()
{
    for design in (1..4).rev() {        //1..=4 iterate the loop four times without loop runs only three times (1..4).rev() iterate backwards
        println!("Design = is {}", design);
    }   //no variable declaration, no increment/decrement, no loop condition
}